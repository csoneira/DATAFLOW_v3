Create a file named TERMINAL_PASSWORD.txt in this directory on each station.
Put one strong terminal password on its first line and restrict it to the bot user:

    chmod 600 TERMINAL_PASSWORD.txt

Alternatively, set MINITRASGO_TERMINAL_PASSWORD in the bot process environment.
Never commit the real password.
