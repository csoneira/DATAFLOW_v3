# 2026-02-17
## Refactoring
---

### Refactoring the pipeline so the config file is not monolithic
I was able to split the config file into several files. Now it's more clear which part affects which.

## Plotting and Ops updates
---

- Added `MASTER/ANCILLARY/PLOTTERS/DEFINITIVE_EXECUTION/definitive_execution_plotter.py` and config file (`definitive_execution_config.json`) to build the new definitive execution PDF per station with the final multi-panel layout (history + zoom + presence + completeness + pie).
- Integrated stage-chain logic in plotting: a basename is only shown at a stage if present in all previous stages; special handling for `MINGO00` stage-0 source (`imported_basenames.csv`) with support for `execution_time`/`execution_timestamp`.
- Updated plot aesthetics/logic requested during iteration: turbo-like ordering by stage, discrete stage legend/colorbar, softened log-like Y behavior for execution time, and aligned right-column panel geometry.
- Added cron generation every 10 minutes for the definitive execution PDF and removed legacy plotters from `CONFIG/add_to_crontab.info`.
- Updated Telegram bot to serve only the new definitive execution report PDF and removed old report commands from help/handlers.
- Added password-gated terminal mode in the Telegram bot: `/terminal <password>` enables command execution, `/exit` disables it, and per-chat `cd` now persists during the session.
- Restart flow verified: bot kill/restart scripts and watchdog path now relaunch the correct bot process.
