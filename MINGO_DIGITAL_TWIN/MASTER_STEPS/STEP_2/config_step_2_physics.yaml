# Active gas area only: controls Step-2 crossing acceptance and Step-3 avalanches.
# Replace these temporary/current values when final measured active dimensions are available.
active_area_bounds_mm:
  # Current configured active span; independent of Step-4 readout dimensions.
  x_min: -116.0
  x_max: 116.0
  y_min: -143.5
  y_max: 143.5

# Variable (selected from the parameter mesh).
z_positions: random
