default:
  default_ncols: 4
  default_max_columns_per_plot: 12

special:
  - name: q_sides
    mode: overlay
    ncols: 4
    series:
      - label: B
        columns: '*_Q_B'
      - label: F
        columns: '*_Q_F'
